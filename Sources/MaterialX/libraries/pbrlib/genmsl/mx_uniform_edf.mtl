#include "lib/mx_closure_type.mtl"

void mx_uniform_edf(ClosureData closureData, vec3 color, thread EDF& result)
{
    if (closureData.closureType == CLOSURE_TYPE_EMISSION)
    {
        result = color;
    }
}
